# CHANGELOG.md is generated by semantic-release (@semantic-release/changelog) in its
# own format; let prettier leave it alone so each release doesn't re-introduce a
# pre-commit failure.
CHANGELOG.md
